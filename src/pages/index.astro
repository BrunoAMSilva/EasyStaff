---
import MusicalScore from "../components/Staff.astro";
import { parseSong } from "../utils/notation-parser";

import SongPicker from "../components/SongPicker.astro";
import BaseLayout from "../layouts/BaseLayout.astro";
const raw = {
	staffs: [
		{
			name: "right",
			notation: `do re re-- mi-- . [re mi] . do-- . . fa-- . si . [mi re] do`,
		},
		{
			name: "left",
			notation: `do-chord--- re-chord--- do-chord---`,
		},
	],
};

const parsed = parseSong(raw);
---

<BaseLayout>
	Escolhe uma música no menu acima.
</BaseLayout>
